# Bounded launch operations review

Read-only checkout review on 2026-09-13. No source edits, builds, live traffic, shared browser use, deployments, external messages, provider changes or backup operations were performed. The review used current repository evidence; older audit status was not carried forward blindly.

1. **Alert delivery and ownership remain unproved.** `.github/workflows/live-data-verification.yml:39` records all source outcomes and uploads failure reports at line 49. This is improved evidence preservation, but there is no configured recipient/delivery receipt or missing-run alert in these files. `docs/launch/readiness.md:16` correctly leaves operations pending. An owner/backup plus one deliberate failure and one missed-run detection are the narrow pilot gate. Proposed hourly HTTP workflow included; recipients remain an actual configuration/delivery test.

2. **Current published data health is a snapshot, not scheduler health.** `app/data-health/page.tsx:65` displays its checked-at timestamp and explicitly explains publication lag. It currently has no age-derived warning. An unchanged 26/26 number can remain on an old deployed build after future checks fail or stop. Proposed probe distinguishes availability from an eight-day snapshot warning; the runbook keeps manual factual-review/cohort dates separate. No new backend is needed for a supervised pilot.

3. **No exact hosted release/rollback record was found.** `build/sites-vite-plugin.ts:27` packages hosting metadata only, `worker/index.ts:29` dispatches image/application requests, and `.github/workflows/ci.yml` tests source without promoting a deployment. The checkout does not establish which hosted version matches a source/artifact hash, a compatible rollback target, or a tested restore operation. Prepare one short release record and rehearse through the real hosting interface; do not assume a generic Wrangler operation is available through Sites.

4. **Existing tests do not prove hosted assets, provider wiring, or classroom capacity.** `tests/route-integrity.test.mjs:46` imports the built Worker with a stub ASSETS fetch. The optimizer uses real `ASSETS` and `IMAGES` bindings in `worker/index.ts:35–38`. The new probe requests both compiled JS and one real image transformation along with dynamic documents. Its load profile is 30 virtual students/120 GETs; no load claim is made until root runs it on the final actual release.

5. **Account integration evidence has improved, with a remaining deployment boundary.** Both `local-account-integration.json` and `hosted-account-integration.json` report six passing real database/session/ownership/revocation/cascade checks and cleanup. Their original runner called the deletion helper directly. Root subsequently extended it and reports local-production HTTP `/api/account` with the hosted DB passed in `hosted-endpoint-integration.json`. Preserve this success; the hosted website Worker route, runtime server-only secret, email/Google/browser flows, and student support/backup process still need their own evidence after deployment. Do not revert the ledger to “no configured project.”

6. **Backups and fallback scope are operational decisions, not build output.** No exercised backup restore or matching application rollback evidence was found. The app separates browser-local research/profile data from account saves; a database backup does not cover browser storage. A code rollback does not undo database writes or migrations. The runbook sets a small restore rehearsal, a compatible prior/current-source fallback, and a proposed 15-minute recovery target rather than claiming recovery already works.

Implemented in `/tmp`: `pilot-probe.mjs`, its pure mocked-network unit tests, the proposed workflow, and the runbook. Nine tests passed. The scripts were not run against local or hosted servers by this reviewer; root is handling the final build and live checks.

Current official documentation checked: [Supabase changelog](https://supabase.com/changelog), [SMTP](https://supabase.com/docs/guides/auth/auth-smtp), [database backups](https://supabase.com/docs/guides/platform/backups), [GitHub schedule behavior](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#schedule), and [Worker rollback limits](https://developers.cloudflare.com/workers/versions-and-deployments/rollbacks/). No changelog-breaking change was found in an API this proposed probe uses; it calls only the app's public GET pages and uses no Supabase Management API.
