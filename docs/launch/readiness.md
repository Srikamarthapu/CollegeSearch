# CollegeSearch school launch acceptance ledger

Updated September 13, 2026. Scope: implement the September 10 audit findings and prepare a reliable school-facing release. **Not yet approved for school-wide production use.** Historical evidence remains in `artifacts/audit/production-readiness-2026-09-10/`.

| Gate | Established evidence | Remaining acceptance work |
|---|---|---|
| Release checks | Current application source f34b2e525fc422297e4cc88efdbc788452f8e56d passed build, types, lint and 335/335 tests; matching CI run 34786172700 passed. Zero dependency advisories were established for the unchanged dependency snapshot. | Private version 7 deployed successfully and passed first/middle/last/only-card keyboard removal checks at 320px width. Earlier comprehensive hosted evidence remains dated to its source. |
| Data integrity | All 26 official source artifacts verified; 163 observations across 24 overlays; changed artifacts reviewed; no numerical/cohort changes across 50 colleges. | Maintain a recurring review/publication owner. A dated snapshot is not a live scheduler result. The page and probe now show when the published verification exceeds the eight-day window, without implying newer reporting cohorts. |
| Student research | Navigation/reload recovery, explicit save, visible-draft export, two-tab conflict detection and explicit resolution passed in the browser. Add-only JSON restore preserved existing research. | Broader browser/device coverage; keep exported files private. |
| Applicant profile | Optional GPA with original scale/weighting, activities and preparation checklist persist locally. Guest/account isolation and omission from matching URLs verified. | Counselor brief download contents, keyboard preview scrolling and clear-confirmation cancellation were checked in browser. Final clear and broader browser coverage remain. No admissions model or validated reach/target/safety classifier is claimed. |
| Preference matching | Residency-aware historical net-price context and consistent outcome/enrollment populations are tested. | Historical averages cannot establish personal affordability or admission outcomes. |
| College preparation | 186 verified official links across 200 action slots for 50 colleges; 14 unavailable slots disclosed. Five calculator-year caveats retained. Student task creation, date, reload and completion passed in browser. | Deadline edit and 100-task backup preview/restore now also pass browser checks, including restoring the original list and reloading. Stale-entry conflict, reload recovery and retaining both task versions now pass a two-tab browser check. Failed canonical recovery is covered by synthetic Storage regressions; broader browser/error coverage remains. Dates are student-entered; no automatic deadline feed or notifications. |
| Responsive accessibility | Mobile navigation, Escape/focus return, 320px planner/profile, and long-name signed-in account layouts checked. | Short landscape menu clipping, nested wheel scrolling, planner/profile focus and backup-review navigation were repaired and exercised; see flow-audit/REPORT.md and final-gates/REPORT.md. Actual enlarged text, reduced motion and additional browser/device coverage remain before broad rollout. |
| Accounts | Dedicated free project; real local and hosted two-user/anonymous isolation, revoked-JWT denial and exact-owner deletion passed (6 checks each). Local production HTTP endpoint to hosted DB also passed 6 checks. Real browser sign-in, explicit import, hosted save persistence and sign-out passed. The exact private hosted website also passed browser sign-in, saved-list reload, sign-out and six HTTP endpoint integration checks. | Custom SMTP and real confirmation/recovery delivery remain. One real-hour token-renewal check across private versions 5 and 6 passed: automatic refresh, provider-confirmed old-token expiry, normal save/reload and exact cleanup. A remote-revocation defect was found and repaired: the actual private browser now preserves pending intent, reverifies on Retry, and replays it after sign-in; both tabs return to guest state. Google stays hidden while unconfigured. |
| Privacy and deletion | Ownership, server revocation/deletion, browser erasure/fencing regressions pass. Deletion confirmation, disabled blank confirmation and cancel checked in browser. | Operator/support contact, effective date, retention/provider disclosures and backup/deletion operating process need owner details. Final destructive browser click was not exercised. |
| Operations | Version 6 local smoke passed 16 checks, including My deadlines. Earlier private hosted smoke passed 15 checks; version 7 received targeted keyboard checks. Historical private hosted classroom probe: 120/120 document requests, 30 virtual readers, zero failures, p95 913.6 ms. Bounded probe and operations runbook exist. | School-device/network rehearsal, real alert receipt, schedule monitoring, hosted backup recovery and operational ownership. A private application rollback to version 2 and return to version 3 was exercised separately; it is historical and does not restore a database. Identify a compatible rollback target for version 7 before school use. Classroom probe excludes assets/rendering and account writes; it does not establish school-wide capacity. |
| Database recovery | Isolated local logical restore recovered three synthetic Auth users, three sessions and four saves. App schema, grants, four RLS policies and both migrations matched; six ownership, anonymous, revocation and deletion-reconciliation cases passed. Temporary resources were removed and existing Docker resources preserved. | Verify actual hosted/off-site backup availability, retention, restore ownership and post-backup deletion records. No Auth HTTP service was connected to the restored target; provider recovery and school-scale restoration remain unverified. |
| Publication | Version 7 deployed privately from source f34b2e525fc422297e4cc88efdbc788452f8e56d, environment revision 1; matching CI passed. | Preserve private access until the release gates are satisfied. Earlier image fallback, account-endpoint and anonymous-access checks retain their tested release boundaries. |

Ooru is paused with owner approval. ResourceDex is unchanged. CollegeSearch has its own active free Supabase project; no paid upgrade was made.

## Evidence

Current tests, types and lint logs are in `artifacts/launch-2026-09-13/recovery-audit/`; its report and deployment record link the matching successful build/CI. `final-gates/` preserves the preceding 316-test source. The parent `release-{build,tests,typecheck,lint}.log` files remain historical. Dependency and data evidence: `dependency-audit-final.json`, `data-offline-final.log`, `data/`, and `college-links/` in the parent directory. Earlier account evidence: `local-account-integration.json`, `hosted-account-integration.json`, `hosted-endpoint-integration.json`, and `browser-account-sync.json`. The endpoint report's application origin is localhost; its filename does not establish a deployed Worker check. Browser observations and limitations are recorded in `browser-checklist.md` and the focused follow-up reports.

This ledger keeps the full account-enabled launch goal open. Passing local checks or privately publishing a review build does not complete that goal. The optional hourly probe workflow is a proposal only; no alert delivery is claimed.

Private hosted evidence: `private-hosted-smoke.json`, `private-hosted-account-integration.json`, and `private-hosted-classroom.json`. Browser account cleanup is recorded separately. The image endpoint originally returned a valid original image but logged a missing optional service; the final correction preserves the library validation and avoids invoking an absent service.

Current release provenance is in `artifacts/launch-2026-09-13/recovery-audit/remove-focus-deployment.json`; version 6 is preserved in `recovery-audit/deployment.json`; the prior session/scrolling release is in `final-gates/deployment.json`; the preceding accessibility release remains in `flow-audit/deployment.json`. The previous release record and its comprehensive hosted checks remain in `deployment.json` and `private-hosted-final-smoke.json` at the parent evidence directory. Subsequent evidence-only documentation commits do not change the deployed application source named in that record.

## Remaining owner inputs

- Public operator name and support contact, plus the intended retention and deletion process. These were requested and remain unanswered; no public contact was invented from private account details.
- A verified sending domain/SMTP service for actual student email confirmation and recovery. Supabase's default organization-only sender cannot support school onboarding.
- Named maintenance/alert recipients and the private backup destination/restore responsibility. The probe and runbook are implemented, but real alert delivery and database recovery cannot be claimed from configuration alone.

School Wi-Fi/device testing and broader accessibility/browser coverage remain required validation work. The app currently reviews 50 colleges and discloses 14 unavailable official-resource slots; it is not a comprehensive national directory. The applicant profile is a local preparation foundation and does not infer admission odds.

The historical accessibility release and its checks are preserved in `artifacts/launch-2026-09-13/flow-audit/deployment.json` and `flow-audit/REPORT.md`.

Additional scrolling, data-recency and revoked-session evidence is in `artifacts/launch-2026-09-13/final-gates/REPORT.md`. The fix retains pending changes instead of accepting RLS-filtered empty responses as successful sync. All disposable hosted accounts used by this follow-up were removed and their saved rows verified absent.

Synthetic database-recovery evidence is in `artifacts/launch-2026-09-13/recovery-audit/database-restore/REPORT.md`, with the archive hash, source/target app-schema manifests, SQL assertions and exact cleanup report. This local rehearsal does not establish a hosted backup or public-launch readiness.

Initial GitHub inspection found `main` unprotected. Strict CI protection was subsequently applied and verified: the existing Actions check is required, administrators are included, and force pushes/deletion are disabled; no reviewer is required. Main is now protected. Weekly verification remains only on the review branch; actual default-branch scheduling and alert receipt remain unverified. See `artifacts/launch-2026-09-13/recovery-audit/github-release-controls.md` and `main-protection-check.json`.

Recovery-read ordering, explicit profile-clear outcomes, planner stale-form recovery and guarded focus are documented in `artifacts/launch-2026-09-13/recovery-audit/REPORT.md`. Version 6 passed the 16-request local smoke. Current version 7 passed build, types, lint, 335 tests and targeted hosted removal checks. Natural hosted session renewal across versions 5 and 6 passed; the disposable account, sessions, saved rows and credential manifest were removed.

The four temporary guest bookmarks used for the version 7 keyboard check were removed; the original empty shelf was verified, the viewport override was reset and the private preview returned home. No local preview or timed session-test process remains active.
